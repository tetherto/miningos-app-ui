import styled from 'styled-components'

import { flexRow } from '../../../../app/mixins'

export const AlarmTabContainer = styled.div`
  ${flexRow};
  row-gap: 16px;
  flex-wrap: wrap;
  margin-top: 20px;
`
