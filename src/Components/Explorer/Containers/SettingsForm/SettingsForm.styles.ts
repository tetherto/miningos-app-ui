import Row from 'antd/es/row'
import styled from 'styled-components'

import { COLOR } from '@/constants/colors'

export const SettingsFormSection = styled(Row)`
  padding: 20px;
`

export const SettingsFormInputTitle = styled.div`
  color: ${COLOR.LIGHT};
  font-size: 12px;
  font-style: normal;
  font-weight: 600;
  line-height: normal;
  padding-bottom: 8px;
`
