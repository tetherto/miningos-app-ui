import Row from 'antd/es/row'
import styled from 'styled-components'

import { COLOR } from '@/constants/colors'

export const ThresholdSettingsDemoTitle = styled.div`
  color: ${COLOR.COLD_ORANGE};
  font-size: 18px;
  font-style: normal;
  font-weight: 600;
  line-height: 25px;
  text-transform: uppercase;
`

export const ThresholdSettingsDemoContent = styled(Row)`
  padding: 20px;
`
