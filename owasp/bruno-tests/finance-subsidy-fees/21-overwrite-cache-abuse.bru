meta {
  name: [21] Resource — overwriteCache Abuse
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/auth/finance/subsidy-fees?start={{startTs}}&end={{endTs}}&period=daily&overwriteCache=true
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("returns 200 (overwriteCache accepted)", function() {
    expect(res.status).to.equal(200);
  });

  test("response time under 15s", function() {
    expect(res.responseTime).to.be.below(15000);
  });
}
