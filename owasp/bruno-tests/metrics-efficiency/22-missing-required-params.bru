meta {
  name: [22] Validation — Missing Required Params
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/auth/metrics/efficiency
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("returns 400 when required params missing", function() {
    expect(res.status).to.be.oneOf([400, 500]);
  });

  test("error message indicates missing parameters", function() {
    const body = JSON.stringify(res.body).toLowerCase();
    const indicatesError = body.includes('required') ||
      body.includes('missing') ||
      body.includes('err_') ||
      res.status >= 400;
    expect(indicatesError).to.be.true;
  });
}
